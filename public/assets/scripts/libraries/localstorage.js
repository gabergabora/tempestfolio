// Check if browser supports local storage

// Add to localstrorage

// retrieve from locallstorage

//update localstorage
Storage = {
	darkmode: true,
	isFirstTime: true,
};

StorageString = JSON.stringify(Storage);

console.log(StorageString);
